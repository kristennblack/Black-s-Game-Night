{
  "name": "black-family-game-night",
  "version": "1.10.0-prop-visual-test",
  "private": true,
  "type": "module",
  "description": "Black Family Game Night v1.10.0 test - deep Prop Hunt visual overhaul with realistic scene plates, dense illustrated prop sprites and identical decoy/disguise art; retains Family Sabotage and all prior fixes.",
  "scripts": {
    "test": "node --test",
    "check": "node --check worker.mjs && node --check gameEngine.mjs && node --check extraGames.mjs && node --check public/app.js && node --check public/prop-hunt-3d.js && node --check public/family-sabotage.js && node --check public/birthday-climb.js && node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
