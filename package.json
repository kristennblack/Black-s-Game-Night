{
  "name": "black-family-game-night",
  "version": "1.1.0-tryout.1",
  "private": true,
  "type": "module",
  "description": "Black Family Game Night v1.1.0 tryout - v1.0.10 Lodge plus Family Mystery and Family Prop Hunt playable previews.",
  "scripts": {
    "test": "node --test",
    "check": "node --check worker.mjs && node --check gameEngine.mjs && node --check extraGames.mjs && node --check public/app.js && node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
