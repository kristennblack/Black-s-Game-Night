{
  "name": "black-family-game-night",
  "version": "1.6.0-prop-mystery-test",
  "private": true,
  "type": "module",
  "description": "Black Family Game Night v1.6.0 test - Prop Hunt controls/art overhaul plus deeper Family Mystery board and deduction UX, while retaining v1.5 polish and v1.2.6 mobile/card fixes.",
  "scripts": {
    "test": "node --test",
    "check": "node --check worker.mjs && node --check gameEngine.mjs && node --check extraGames.mjs && node --check public/app.js && node --check public/prop-hunt-3d.js && node --check public/birthday-climb.js && node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
