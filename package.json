{
  "name": "black-family-game-night",
  "version": "1.2.0-launch",
  "private": true,
  "type": "module",
  "description": "Black Family Game Night v1.2.0 launch candidate - animated Black Family Lodge, 18 original multiplayer games, Family Mystery, 3D Prop Hunt, John's Birthday Seat, computer players and share links.",
  "scripts": {
    "test": "node --test",
    "check": "node --check worker.mjs && node --check gameEngine.mjs && node --check extraGames.mjs && node --check public/app.js && node --check public/prop-hunt-3d.js && node --check public/birthday-climb.js && node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
