Black Family Game Night v1.3.0-real3d
Date: 2026-08-23

RESULT: 143 / 143 automated tests passing

NEW PROP HUNT FOUNDATION COVERAGE
- A wall AABB is mathematically hit before a target behind it.
- Third-person camera distance is shortened by intervening world geometry.
- Solid walls block character movement.
- Jump input can trigger automatic mantle on a climbable ledge within the allowed height.
- Solid ceilings stop upward head movement.
- Hunter assignment rotates across rounds and scales to two hunters in larger rooms.
- Network movement snapshots are clamped/sanitized.
- Server hit acceptance validates hunter -> live hider and maximum range.
- Prop Hunt source requires WebGLRenderer, PerspectiveCamera, Raycaster, tone mapping and shadows.
- Prop Hunt source explicitly contains no Canvas 2D getContext('2d') renderer.
- Human and dog 3D hierarchy builders and skeleton-attached weapons are present.
- Segmented walls, windows, collision-backed glass and ceilings are present.
- Shooting and bot sight both raycast against world geometry.
- Mobile/desktop actions include Aim, Shoot, Jump/Auto-Mantle, Sprint, Prop, Flash, Decoy, Lock and joystick movement.
- Lodge creates a dedicated private Prop Hunt room rather than opening local-only play.
- Dedicated PropHuntRoom create/join/ready/start flow is exercised with an in-memory SQLite-compatible test context.
- Cloudflare configuration includes the PROP_HUNT binding, PropHuntRoom SQLite migration and observability.
- PropHuntRoom uses Durable Object hibernatable WebSocket primitives and SQL persistence.

REGRESSION COVERAGE RETAINED
- All 18 original multiplayer games create, join, seat, ready and start.
- All 18 original games can launch solo with Easy/Medium/Hard computer players.
- Existing card/trick/extra-game rules and privacy tests.
- Existing GameHub persistence and room state.
- Chat, reactions, avatars, game switching, share links and private hands.
- Family Mystery board/movement coverage.
- John's Birthday Seat coverage.
- Lodge cabin and animated-fire coverage.

STATIC VALIDATION
- All project .js and .mjs files pass Node syntax validation.
- wrangler.jsonc parses as valid JSON and contains both Durable Object bindings/migrations.

RUNTIME QA NOTE
A GPU/browser WebGL gameplay session could not be executed to completion inside this container because the available headless Chromium process did not complete reliably. The package therefore has strong source/unit/integration coverage but should still receive the normal production smoke test on one desktop browser and one phone after deployment, especially for GPU/WebGL rendering, touch feel and cross-device WebSocket latency.
