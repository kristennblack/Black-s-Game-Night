import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(new URL('..',import.meta.url).pathname);
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('U1 arcade files are self-contained and integrated',()=>{const app=read('public/app.js'),sw=read('public/sw.js');for(const [f,key] of [['public/space-shooter.html','starpatrol'],['public/rocket-gap.html','rocketgap'],['public/neon-snake.html','snake']]){const s=read(f);assert.match(s,/<canvas/);assert.match(s,/<style>/);assert.match(s,/<script>/);assert.doesNotMatch(s,/<script[^>]+src=/);assert.doesNotMatch(s,/<link[^>]+stylesheet/);assert.match(app,new RegExp(key));assert.match(sw,new RegExp('/'+path.basename(f).replace(/[.*+?^${}()|[\]\\]/g,'\\$&')))}});
test('space shooter has capped objects, limited fire and rAF',()=>{const s=read('public/space-shooter.html');assert.match(s,/bullets\.length>=55/);assert.match(s,/enemies\.length>=28/);assert.match(s,/fireT=\.12/);assert.match(s,/requestAnimationFrame/);assert.match(s,/asteroid/);assert.match(s,/hunter/)});
test('rocket uses fixed step, shrinking safe gap and local high score',()=>{const s=read('public/rocket-gap.html');assert.match(s,/FIX=1\/120/);assert.match(s,/Math\.max\(145,188-score\*1\.6\)/);assert.match(s,/localStorage/);assert.match(s,/requestAnimationFrame/)});
test('snake uses 20x20, queued turns, reversal guard and speed ramp',()=>{const s=read('public/neon-snake.html');assert.match(s,/N=20/);assert.match(s,/queue\.length<2/);assert.match(s,/d\.x===-base\.x&&d\.y===-base\.y/);assert.match(s,/Math\.floor\(score\/5\)/);assert.match(s,/localStorage/)});
