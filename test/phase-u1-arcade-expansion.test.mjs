import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(new URL('..',import.meta.url).pathname);
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('legacy U1 rocket and snake remain self-contained while Kelsi replacement uses shared Phase W platform',()=>{const app=read('public/app.js'),sw=read('public/sw.js');for(const [f,key] of [['public/rocket-gap.html','rocketgap'],['public/neon-snake.html','snake']]){const s=read(f);assert.match(s,/<canvas/);assert.match(s,/<style>/);assert.match(s,/<script>/);assert.doesNotMatch(s,/<script[^>]+src=/);assert.doesNotMatch(s,/<link[^>]+stylesheet/);assert.match(app,new RegExp(key));assert.match(sw,new RegExp('/'+path.basename(f).replace(/[.*+?^${}()|[\]\\]/g,'\\$&')))}const k=read('public/space-shooter.html');assert.match(k,/Kelsi's Rock 'n' Roll Rescue/i);assert.match(k,/phase-w-platform/);assert.match(app,/starpatrol/);assert.match(sw,/space-shooter\.html/)});
test('Kelsi rock rescue has rocks, bonuses, mud hazards, touch control and rAF',()=>{const s=read('public/space-shooter.html');assert.match(s,/rock/i);assert.match(s,/shiny/i);assert.match(s,/heart/i);assert.match(s,/mud/i);assert.match(s,/pointermove/);assert.match(s,/requestAnimationFrame/);assert.doesNotMatch(s,/asteroid|hunter|bullets\.length>=55/i)});
test('rocket uses fixed step, shrinking safe gap and local high score',()=>{const s=read('public/rocket-gap.html');assert.match(s,/FIX=1\/120/);assert.match(s,/Math\.max\(145,188-score\*1\.6\)/);assert.match(s,/localStorage/);assert.match(s,/requestAnimationFrame/)});
test('snake uses 20x20, queued turns, reversal guard and speed ramp',()=>{const s=read('public/neon-snake.html');assert.match(s,/N=20/);assert.match(s,/queue\.length<2/);assert.match(s,/d\.x===-base\.x&&d\.y===-base\.y/);assert.match(s,/Math\.floor\(score\/5\)/);assert.match(s,/localStorage/)});
