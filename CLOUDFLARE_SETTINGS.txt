CLOUDFLARE BUILD SETTINGS - v1.3.0-real3d
=========================================
Build command:   None / blank
Deploy command:  npx wrangler deploy
Root directory:  /
Production branch: main

DURABLE OBJECTS
- Existing binding: GAME_HUB -> GameHub
- New Prop Hunt binding: PROP_HUNT -> PropHuntRoom
- Migration v1: GameHub (retain)
- Migration v2: PropHuntRoom (new SQLite class)

OBSERVABILITY
wrangler.jsonc enables Workers observability/logs plus sampled traces.

FOR THE EXISTING LIVE APP
- Keep the existing GitHub repository and hidden .git folder.
- Replace tracked project files with this v1.3.0 package.
- Commit and Push origin.
- Cloudflare should deploy from the existing Git integration.
- Do not disconnect/reconnect Cloudflare unless the existing integration is actually broken.
